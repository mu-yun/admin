spring:
  application:
    name: spring-boot
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true



#  devtools:
#    restart:
#      additional-exclude:
